# OBJ

Part of the **MDT** (Mission Data Table).

Lists the player faction's currently active objectives with status, completion percent, and
collapsible per-objective position sub-rows (grid label + live range).
